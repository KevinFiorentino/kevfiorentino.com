---
import { getEntry } from 'astro:content';
import { languages } from '@shared/i18n/ui';
import { getLangFromUrl, useTranslations } from '@shared/i18n/utils';
import MainLayout from '@shared/layouts/MainLayout.astro';
import Presentation from '@shared/components/home/Presentation.astro';
import Projects from '@shared/components/home/Projects/Projects';
import TechJourney from '@shared/components/home/Techs/TechJourney';
import Blog from '@shared/components/home/Blog.astro';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const currentUrl = Astro.url.pathname;
const currentLang = currentUrl.split('/')[1];

export async function getStaticPaths() {

  const pages: any[] = [];
  const posts: any[] = [];

  const p1 = await getEntry('blockchain', 'web2-vs-web3');
  const p2 = await getEntry('solana', 'behind-solana');
  const p3 = await getEntry('ethereum', 'erc20-token');
  const p4 = await getEntry('solidity', 'reentrancy-attack');
  const p5 = await getEntry('openzeppelin', 'escrow');
  const p6 = await getEntry('bitcoin', 'architecture');

  posts.push(p1, p2, p3, p4, p5, p6);

  Object.keys(languages).forEach((l: string) => {
    pages.push({
      params: { lang: l },
      props: {
        posts: posts,
      },
    });
  });

  return pages;
}

const { posts } = Astro.props;
---

<MainLayout
  title="Home | Kevin Fiorentino"
  description={`Kevin Fiorentino's professional website.`}
  image="/kevin2025.jpg"
  addWidth={false}
>

  <Presentation></Presentation>

  <Projects
    client:load
    currentLang={currentLang}
  ></Projects>

  <TechJourney
    client:load
    currentLang={currentLang}
  ></TechJourney>

  <br><br>

  <Blog
    posts={posts}
  ></Blog>

</MainLayout>
